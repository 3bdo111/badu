import type { Review } from "@/lib/types/review";

/**
 * Customer reviews.
 *
 * Intentionally empty: BADU has not launched yet, and no verified
 * customer reviews exist. Real reviews will be added here (and later
 * served from the backend) — the Reviews section renders a designed
 * pre-launch state until then.
 */
export const reviews: Review[] = [];
