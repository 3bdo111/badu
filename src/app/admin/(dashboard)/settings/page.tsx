import { SettingsEditor } from "@/components/admin/cms/SettingsEditor";

export const metadata = {
  title: "Global Site Settings | BADU Admin",
};

export default function AdminSettingsPage() {
  return <SettingsEditor />;
}
